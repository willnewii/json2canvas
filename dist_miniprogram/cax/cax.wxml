<canvas 
    class="cax-canvas"
    bindtouchstart="touchStart" 
    bindtouchmove="touchMove" 
    bindtouchend="touchEnd" 
    canvas-id="{{ id }}" 
    style="border: 1px solid;width: {{width}}px; height: {{height}}px;"
    wx:if="{{showCanvas}}">
    <slot></slot>
</canvas>